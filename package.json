{
  "name": "slon-app",
  "private": true,
  "description": "Тестовое окружение для CI (Playwright). На деплой не влияет — сайт остаётся статическими файлами без сборки.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.48.0"
  }
}
