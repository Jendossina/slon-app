<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Разрешения на геолокацию убраны вместе с гео-проверкой прихода:
         на части телефонов местоположение не определялось вовсе, и сотрудник
         не мог отметиться. Место подтверждает видео с камеры. -->

    <!-- Видео прихода снимается ВНУТРИ приложения (getUserMedia + MediaRecorder),
         чтобы ролик не оставался в галерее телефона и приложение не сворачивалось
         в системную камеру — в фоне Android спокойно выгружал его вместе с
         недогруженной отметкой. Для съёмки в WebView разрешение нужно самому
         приложению: Capacitor запрашивает его у человека при первой записи.
         Фото задач и чек-листов по-прежнему делает системная камера по интенту. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Приложению нужен доступ в интернет: без сети оно показывает заглушку -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>
