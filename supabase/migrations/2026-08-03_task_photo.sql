-- Фото к задаче при постановке.
--
-- report_url — это фотоотчёт СОТРУДНИКА о выполнении. Здесь обратное: снимок от
-- того, кто задачу ставит («вот эта полка», «вот такой ценник»), чтобы не
-- объяснять словами. Поэтому отдельная колонка, а не переиспользование отчёта.
--
-- Файл лежит в том же бакете task-reports в корне — значит, попадает под
-- «очистку медиа старше N дней» из админ-панели наравне с отчётами. Это
-- намеренно: снимок нужен, пока задача живая.

alter table public.tasks add column if not exists photo_url text;
