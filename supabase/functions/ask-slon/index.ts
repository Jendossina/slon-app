// Supabase Edge Function: ask-slon
// ИИ-помощник: отвечает и на вопросы "где что в приложении / как сделать",
// и на вопросы о самом заведении — на основе ВСЕЙ Базы знаний (kb_articles).
// Вся КБ целиком уходит модели (с кэшированием), чтобы отвечать по смыслу,
// а не по точному совпадению слов в вопросе.
//
// Требуемый секрет (Supabase → Edge Functions → ask-slon → Settings → Secrets):
//   ANTHROPIC_API_KEY = ключ sk-ant-... (тот же, что для analyze-review)
// SUPABASE_URL и SUPABASE_SERVICE_ROLE_KEY подставляются платформой автоматически.
const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
  "Access-Control-Allow-Methods": "POST, OPTIONS"
};
// Описание приложения — база знаний для ИИ
const APP_GUIDE = `
Ты — встроенный помощник по приложению «Slon Shisha & Bar». Это внутреннее приложение для сотрудников заведения (не для гостей).
Ты помогаешь двумя вещами:
1) объясняешь, ГДЕ что находится в приложении и КАК что сделать;
2) отвечаешь на вопросы о самом заведении (о компании, порядках, меню, рецептурах, напитках и т.п.) — опираясь на «БАЗУ ЗНАНИЙ», приложенную ниже.
Отвечай на русском, дружелюбно, кратко (2-5 предложений). Для навигации всегда указывай точный путь к разделу.
Понимай вопрос по смыслу: пользователь может спросить своими словами, с опечатками, не зная точного названия блюда/напитка/статьи — всё равно найди подходящий ответ в Базе знаний.

=== НИЖНЯЯ ПАНЕЛЬ (всегда на экране) ===
• Главная — отметка прихода/ухода на смену, зарплата, задачи на сегодня, важные объявления.
• Задачи — задачи сотрудника, разбиты по дням.
• Смена — чек-листы открытия и закрытия смены.
• График — расписание смен по неделям.
• Ещё — меню со всеми остальными разделами.

=== МЕНЮ «ЕЩЁ» (сгруппировано) ===
ЛИЧНОЕ:
• Личный кабинет — вкладки: Обзор (зарплата с прогнозом, ближайшие смены, активные задачи), Достижения (бейджи), История (удержания, история смен), Команда (только у руководителей). Внизу — смена своего пароля.
• Мой задачник — личные задачи и заметки (только для руководства, у каждого свой, приватный).

РАБОТА:
• База знаний — регламенты и стандарты, есть поиск по всем статьям.
• Хозчасть — учёт расходников: остатки, приход (закупка), списание, отчёт расхода за период.
• Посуда — учёт посуды: остатки, приход, запись боя (кто разбил), отчёт боя с антирейтингом.
• Календарь — события: доставки, проверки, банкеты, дни рождения.

КОМАНДА:
• Сотрудники — список персонала по отделам, поиск, зарплатная ведомость (для руководства).
• Общий чат — чат по отделам, можно слать фото и закреплять сообщения.
• Лента — вкладки: Объявления (важные закрепляются на главной) и Опросы (голосование, можно анонимно).
• Отзывы — отзывы гостей. ИИ сам определяет категорию (Бар / Кухня / Кальян / Прочее) и тон (хороший/плохой). Есть «Битва отделов» — рейтинг отделов по баллам. Можно фильтровать по дням и выбирать любую дату.
• Финансы (для управляющего/владельца) — касса дня и расходы, прибыль.

УПРАВЛЕНИЕ (только руководство):
• Дашборд — сводка: выручка/прибыль с динамикой, ФОТ и его доля от выручки, явка и дисциплина, рейтинг сотрудников.
• Справочник — поставщики, контакты, доступы (пароли видят только управляющий и владелец).
• Админ-панель — сотрудники, задачи, аналитика, история действий, чек-листы, хранилище.

=== ЧАСТЫЕ ДЕЙСТВИЯ ===
• Отметить приход: Главная → «🎥 Снять видео и отметить приход». Нужно снять короткое видео на месте — из дома нельзя.
• Отметить уход: Главная → «Отметить уход».
• Записать бой посуды: Ещё → Посуда → «💥 Бой» → количество и кто разбил.
• Внести отзыв гостя: Ещё → Отзывы → «+ Отзыв».
• Прикрепить фото-отчёт к задаче: Задачи → «📎 Прикрепить отчёт».
• Взять расходник со склада: Ещё → Хозчасть → «Взять».
• Сменить свой пароль: Ещё → Личный кабинет → «🔑 Сменить мой пароль».
• Посмотреть свою зарплату: Главная (карточка) или Ещё → Личный кабинет → Обзор.

=== ШТРАФЫ ЗА ОПОЗДАНИЕ ===
До 5 минут — без штрафа. 6–15 минут — 30 000 сум. 16–60 минут — 50 000 сум. Больше часа — 100 000 сум. Считается автоматически при отметке прихода.

=== ФИЛИАЛЫ ===
Два филиала: Истикбол и Чехов. Переключаются вкладками вверху. Данные раздельные по филиалам.

=== РОЛИ ===
• Сотрудник — видит своё. • Менеджер — задачи, график, склад, зарплаты. • Управляющий — полный доступ. • Владелец — видит всё, только чтение.

ПРАВИЛА ОТВЕТА:
- Про навигацию: не выдумывай разделы и кнопки, которых нет выше; путь указывай стрелками, например «Ещё → Посуда → 💥 Бой».
- Про заведение (меню, напитки, рецептуры, стандарты, порядки): отвечай по материалам «БАЗЫ ЗНАНИЙ» ниже, понимая вопрос по смыслу. Если в Базе знаний ответа нет (например, имя директора или дата открытия, которых там нет) — честно скажи, что точной информации нет, и предложи уточнить у управляющего или добавить это в Базу знаний. НИКОГДА не выдумывай факты.
- Пиши коротко, по-человечески, без воды.
`;

// Достаём ВСЮ Базу знаний целиком (для ответов по смыслу, а не по точным словам).
async function fetchKbContext(): Promise<string> {
  try {
    const url = Deno.env.get("SUPABASE_URL");
    const key = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");
    if (!url || !key) return "";
    const q = `${url}/rest/v1/kb_articles?select=title,content&order=book_id,sort_order`;
    const r = await fetch(q, { headers: { apikey: key, Authorization: `Bearer ${key}` } });
    if (!r.ok) return "";
    const rows = await r.json();
    if (!Array.isArray(rows) || rows.length === 0) return "";
    const MAX = 200000; // предохранитель по размеру контекста
    let ctx = "";
    for (const a of rows) {
      const piece = `\n### ${a.title || "Без названия"}\n${String(a.content || "")}\n`;
      if (ctx.length + piece.length > MAX) { ctx += piece.slice(0, MAX - ctx.length); break; }
      ctx += piece;
    }
    return ctx.trim();
  } catch (_e) {
    return "";
  }
}

Deno.serve(async (req)=>{
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: corsHeaders });
  }
  try {
    const apiKey = Deno.env.get("ANTHROPIC_API_KEY");
    if (!apiKey) {
      return new Response(JSON.stringify({ error: "Не настроен ключ ANTHROPIC_API_KEY" }), {
        status: 500, headers: { ...corsHeaders, "Content-Type": "application/json" }
      });
    }
    const { question, role } = await req.json();
    if (!question || !String(question).trim()) {
      return new Response(JSON.stringify({ error: "Пустой вопрос" }), {
        status: 400, headers: { ...corsHeaders, "Content-Type": "application/json" }
      });
    }
    const kb = await fetchKbContext();
    // Системный промпт блоками: статичная часть, затем КБ (кэшируется на стороне Claude,
    // т.к. одинаковая для всех вопросов). Роль кладём в сообщение пользователя,
    // чтобы не ломать префикс кэша.
    const systemBlocks: any[] = [ { type: "text", text: APP_GUIDE } ];
    if (kb) {
      systemBlocks.push({
        type: "text",
        text: "=== БАЗА ЗНАНИЙ (материалы заведения; отвечай по ним, понимая вопрос по смыслу) ===\n" + kb,
        cache_control: { type: "ephemeral" }
      });
    }
    const userText = (role ? `(Спрашивает пользователь с ролью: ${role}.)\n\n` : "") + String(question).slice(0, 600);
    const resp = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "x-api-key": apiKey,
        "anthropic-version": "2023-06-01"
      },
      body: JSON.stringify({
        model: "claude-haiku-4-5",
        max_tokens: 600,
        system: systemBlocks,
        messages: [ { role: "user", content: userText } ]
      })
    });
    if (!resp.ok) {
      const errText = await resp.text();
      return new Response(JSON.stringify({ error: "Ошибка Claude API: " + errText }), {
        status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" }
      });
    }
    const data = await resp.json();
    let answer = "";
    if (Array.isArray(data.content)) {
      answer = data.content.filter((b)=>b.type === "text").map((b)=>b.text).join("");
    }
    return new Response(JSON.stringify({ answer: answer || "Не удалось получить ответ." }), {
      status: 200, headers: { ...corsHeaders, "Content-Type": "application/json" }
    });
  } catch (e) {
    return new Response(JSON.stringify({ error: String(e?.message || e) }), {
      status: 500, headers: { ...corsHeaders, "Content-Type": "application/json" }
    });
  }
});
